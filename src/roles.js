/**
 * ENUM values for roles. 
 * Set string values like they are in the backend, so we can use it in frontend
 */

export default {
    // front-end roles
    ALL: 'ROLE_ALL',
    GUEST: 'ROLE_GUEST',
    LOGGED: 'ROLE_LOGGED',

    // roles from back-end
    ADMIN: 'ROLE_ADMIN',
};