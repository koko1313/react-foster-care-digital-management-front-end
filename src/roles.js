/**
 * ENUM values for roles. 
 * Set string values like they are in the backend, so we can use it in frontend
 */

export default {
    ALL: 'ROLE_ALL',
    ADMIN: 'ROLE_ADMIN',
    GUEST: 'ROLE_GUEST',
};