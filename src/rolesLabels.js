/**
 * Roles labels, so we can show them in frontend
 */

const rolesLabels = new Map();

rolesLabels.set("ROLE_ADMIN", "Администратор");
rolesLabels.set("ROLE_OEPG", "Служител ОЕПГ");

export default rolesLabels;